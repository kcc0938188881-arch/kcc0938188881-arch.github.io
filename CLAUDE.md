# 義鼎不動產 — 專案須知

## 聯絡資訊（產出對外素材時一律使用這一組）

| 項目 | 內容 |
|---|---|
| 公司 | 義鼎不動產有限公司 |
| 負責人 | 郭志誠（Jason Kuo） |
| 電話 | 0938-188-881 |
| **LINE ID** | **KCCAA** |
| Email | kcc.0938188881@gmail.com |
| YouTube 頻道 | @JasonKuo1881 |
| 證號 | 不動產營業員 108登字第342452號 |

⚠️ `@JasonKuo1881` 是 **YouTube 頻道**，**不是** LINE ID。
兩者曾經被混用過，製作名片、字卡、說帖、DM 時務必分清楚。

對外聯絡區塊的標準寫法（影片字卡、說帖、DM 一律照這個順序）：

```
義鼎不動產有限公司
郭志誠
0938-188-881　LINE：KCCAA
不動產營業員 108登字第342452號
```

**公司名之外一定要有「郭志誠」三個字**，只掛公司名不算完整。

## 影片製作工具

`scripts/` 底下有兩支土地物件影片腳本，都需要 ffmpeg：

| 腳本 | 用途 |
|---|---|
| `make-land-video.sh` | 等長分鏡的簡易版，適合快速出一支照片影片 |
| `make-land-film.sh` | 分鏡表驅動，每個鏡頭可各自指定秒數與特效，適合有段落結構的正式影片 |

用法與參數見 `scripts/README-video.md`。分鏡表範例在 `scripts/AVI142-boards/`。

### 已知的 ffmpeg 陷阱（改腳本前先看）

- **`drawbox` 的 x/y/w/h 表達式只在初始化求值一次**，放 `t` 進去不會有動畫。
  需要掃出、展開等動畫時，要拆成多段靜態色塊用 `enable` 接力顯示（見 `sweep()`）。
- **`drawtext` 的 x/y/alpha/text 則是逐幀求值**，動畫放這裡沒問題。
- **`zoompan` 只能餵一幀**（`-loop 1 -framerate 1 -t 1`），否則 `on` 不會從 0 起算，
  動畫時序會錯，而且每幀重算縮放會拖慢好幾倍。
- 縮放用**幀號線性計算**，不要用 `zoom+0.0004` 累加，累加會有取整抖動。
- **`zoompan` 先輸出 2 倍尺寸再 lanczos 縮回**，整數取整的跳動會被縮小一半並被插值平均掉，
  這是消除運鏡抖動最有效的一步（實測停格幀可降到 0）。
- **不要讓任何鏡頭完全靜止**。圖表類給 `1.0 → 1.05` 的極輕微縮放，
  文字不會晃，但畫面不會「死住」——完全靜止是觀眾感受到卡頓的主因。
- **運鏡幅度要大到每幀位移超過 1 像素**，否則整數取整會產生「停一格、跳一格」。
  算式：`zoompan 輸出寬 × (1 − 1/縮放倍率) ÷ 總幀數 > 1`。
  7 秒 @60fps（420 幀）配 3840 寬輸出時，一般鏡頭需 ZMAX ≥ 1.15、圖表需 ZSTILL ≥ 1.05。
  秒數拉長或 fps 提高都會讓每幀位移變小，反而更容易卡——調參數時要重算。

## 對外文案的紅線

- 只寫謄本、地籍圖、公開資料佐證得了的事實；不確定就標「待查證」或留白。
- 不杜撰成交價、報酬率、政府核准狀態；不把「尚未核定」寫成既成事實。
- 不用「保證」「穩賺」「必漲」「零風險」。
- AI 生成或模擬畫面一律標示「示意圖・非實景」。
- 對外文案結尾必附「投資人須留意事項」。
- 謄本、地籍圖入鏡時，地主姓名、身分證字號、住址一律遮蔽。
- 價格資訊不放進會被隨手轉發的影片；說帖是否揭露價格，每案先確認。
